<%= form_for(@contas_receber) do |f| %>
  <% if @contas_receber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contas_receber.errors.count, "error") %> prohibited this contas_receber from being saved:</h2>

      <ul>
      <% @contas_receber.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :Cliente_id, :class => 'control-label' %>
    <div class="controls form-inline">
      <%= f.collection_select :Cliente_id, Cliente.all, :id, :nome, { include_blank: true }, class: 'form-control' %>
      <%= link_to fa_icon('user-plus', text: "Novo Cliente"), new_cliente_path, :class => "btn btn-success" %>
    </div>
    <%= error_span(@contas_receber[:Cliente_id]) %>
  </div>

  <div class="form-group">
    <%= f.label "Data de Emissão" %><br>
    <%= f.text_field :dataEmissao, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :valor %><br>
    <%= f.text_field :valor, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :vencimento %><br>
    <%= f.text_field :vencimento, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :juros %><br>
    <%= f.text_field :juros, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :multa %><br>
    <%= f.text_field :multa, :class => "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar", :id => 'salvar', :class => "btn btn-success btn-lg" %>
    <%= link_to fa_icon('chevron-left'), contas_recebers_path, :class => "btn btn-warning btn-lg",  "data-toggle" => "tooltip","data-placement" => "top", :title => "Voltar"  %>
  </div>
<% end %>
